[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "promete-strategy-bundle"
version = "0.2.0"
description = "Portable strategy bundles and authenticated ProfinAI import CLI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.5,<3", "httpx>=0.26,<1"]

[project.optional-dependencies]
export = ["numpy>=1.26", "pandas>=2", "pyarrow>=15", "stable-baselines3>=2.7", "sb3-contrib>=2.7"]
test = ["pytest>=8,<9", "build>=1,<2", "numpy>=1.26"]

[project.scripts]
strategy-bundle = "promete_strategy_bundle.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
